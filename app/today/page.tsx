import { GuestPage } from "@/components/layout/guest-page"; import { TodayExperience } from "@/components/guest/today-experience"; import { T } from "@/components/i18n/language-provider"; import { getTodayActivities } from "@/lib/services/hotel-service";
export default async function TodayPage(){const items=await getTodayActivities();return <GuestPage eyebrow={new Intl.DateTimeFormat("fr-FR",{dateStyle:"full"}).format(new Date())} title={<T k="todayAt"/>} intro="Sports, famille et animations : ce qui se passe maintenant et ensuite."><TodayExperience items={items.map(i=>({...i,placeSlug:i.place?.slug??null}))}/></GuestPage>}
