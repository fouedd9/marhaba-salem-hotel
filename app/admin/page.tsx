import Link from "next/link"; import { Activity, Bell, Building2, Store } from "lucide-react"; import { AdminShell } from "@/components/admin/admin-shell"; import { getAdminDashboard } from "@/lib/services/hotel-service";
export default async function AdminPage(){const data=await getAdminDashboard();const metrics=[{label:"Activités aujourd’hui",value:data.counts.activities,icon:Activity},{label:"Annonces actives",value:data.counts.announcements,icon:Bell},{label:"Lieux publiés",value:data.counts.places,icon:Building2},{label:"Établissements",value:data.counts.restaurants,icon:Store}];return <AdminShell title="Vue d’ensemble"><div className="mb-8 flex flex-wrap items-end justify-between gap-4"><div><p className="text-sm text-[#70807c]">Données en direct</p><h2 className="font-display mt-1 text-3xl md:text-4xl">Ce qui se passe aujourd’hui.</h2></div><Link href="/admin/announcements" className="min-h-11 rounded-xl bg-[#0d5b59] px-5 py-3 text-sm font-bold text-white">Créer une annonce</Link></div><section className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{metrics.map(({label,value,icon:Icon})=><article key={label} className="rounded-2xl border border-[#dfe4e1] bg-white p-5"><div className="flex items-center justify-between"><span className="text-sm font-semibold text-[#70807c]">{label}</span><span className="grid size-10 place-items-center rounded-xl bg-[#e8efea] text-[#0d5b59]"><Icon size={18}/></span></div><p className="mt-6 text-3xl font-bold">{value}</p></article>)}</section><section className="mt-6 grid gap-6 xl:grid-cols-2"><article className="rounded-2xl border border-[#dfe4e1] bg-white p-5"><div className="flex justify-between"><h3 className="font-display text-2xl">Programme</h3><Link href="/admin/activities" className="text-sm font-bold text-[#0d5b59]">Gérer</Link></div>{data.activities.slice(0,5).map(a=><div key={a.id} className="flex gap-4 border-b py-4"><strong>{new Date(a.startAt).toLocaleTimeString("fr-FR",{hour:"2-digit",minute:"2-digit"})}</strong><div><p className="font-semibold">{a.title}</p><p className="text-xs text-[#70807c]">{a.location}</p></div></div>)}</article><article className="rounded-2xl bg-[#173d3c] p-6 text-white"><p className="text-xs font-bold uppercase tracking-wider text-[#f4ad99]">Annonce mise en avant</p><h3 className="font-display mt-3 text-2xl">{data.announcements[0]?.title??"Aucune annonce"}</h3><p className="mt-3 text-sm leading-relaxed text-white/65">{data.announcements[0]?.content??"Créez une annonce pour informer les clients."}</p></article></section></AdminShell>}
