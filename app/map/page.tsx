import { GuestPage } from "@/components/layout/guest-page"; import { InteractiveResortMap } from "@/components/map/interactive-resort-map";
export default async function MapPage({searchParams}:{searchParams:Promise<{destination?:string}>}){const {destination}=await searchParams;return <GuestPage eyebrow="Du lobby à la plage, sans hésiter" title="Explorer l’hôtel" intro="Choisissez votre destination : le plan vous indique le chemin depuis le QR code que vous avez scanné."><InteractiveResortMap initialDestination={destination}/></GuestPage>}
