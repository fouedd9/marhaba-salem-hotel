import { GuestPage } from "@/components/layout/guest-page"; import { InteractiveResortMap } from "@/components/map/interactive-resort-map"; import { T } from "@/components/i18n/language-provider";
export default async function MapPage({searchParams}:{searchParams:Promise<{destination?:string}>}){const {destination}=await searchParams;return <GuestPage eyebrow="Marhaba Salem Resort" title={<T k="explore"/>} intro={<T k="mapIntro"/>}><InteractiveResortMap initialDestination={destination}/></GuestPage>}
