import { GuestPage } from "@/components/layout/guest-page";
import { ActivityTimeline } from "@/components/guest/activity-timeline";
import { getTodayActivities } from "@/lib/services/hotel-service";
export default async function ActivitiesPage(){const activities=await getTodayActivities();return <GuestPage eyebrow={new Intl.DateTimeFormat("fr-FR",{dateStyle:"full"}).format(new Date())} title="Activités & divertissements" intro="Animations quotidiennes, sports, spectacles, soirées à thème et musique live pour toute la famille."><ActivityTimeline items={activities}/><div className="mt-10 rounded-[1.75rem] bg-[#173d3c] p-6 text-white"><p className="text-xs font-bold uppercase tracking-wider text-[#f4ad99]">Équipements & loisirs</p><h2 className="font-display mt-2 text-3xl">Bougez à votre rythme.</h2><p className="mt-3 text-white/70">Tennis, basketball, beach-volley, mini-golf, tennis de table, tir à l’arc, aérobic et billard.</p></div></GuestPage>}
