import Image from "next/image"; import Link from "next/link"; import { Clock,Leaf,MapPin,Utensils } from "lucide-react"; import { GuestPage } from "@/components/layout/guest-page"; import { LocationLink } from "@/components/location/location-link"; import { T } from "@/components/i18n/language-provider"; import { getDining } from "@/lib/services/hotel-service"; import { getOpeningState } from "@/lib/utils/opening-hours";
const meals={BREAKFAST:"Petit-déjeuner · Breakfast",LUNCH:"Déjeuner · Lunch",DINNER:"Dîner · Dinner",SNACK:"En-cas · Snacks"};
export default async function DiningPage(){const {restaurants,menus}=await getDining();return <GuestPage eyebrow={<T k="whatToEat"/>} title={<T k="venues"/>} intro="Menus du jour et établissements du Marhaba Salem."><section><div className="mb-5 flex items-end justify-between"><div><p className="text-xs font-bold uppercase tracking-wider text-[#d66045]"><T k="menuToday"/></p><h2 className="font-display mt-1 text-3xl"><T k="whatToEat"/></h2></div><Utensils className="text-[#0d5b59]"/></div>{menus.length?<div className="grid gap-4 lg:grid-cols-3">{menus.map(menu=><article key={menu.id} className="rounded-2xl bg-white p-5 shadow-sm"><p className="text-xs font-bold uppercase tracking-wide text-[#d66045]">{meals[menu.mealType]}</p><h3 className="font-display mt-1 text-2xl">{menu.title}</h3><div className="mt-4 divide-y">{menu.items.map(item=><div key={item.id} className="py-3"><div className="flex justify-between gap-2"><strong className="text-sm">{item.name}</strong>{item.isVegetarian&&<Leaf size={15} className="text-[#0d5b59]"/>}</div>{item.description&&<p className="mt-1 text-xs text-[#65716c]">{item.description}</p>}</div>)}</div></article>)}</div>:<div className="rounded-2xl border border-dashed border-[#cfc8bb] bg-white/classic p-8 text-center text-[#65716c]"><T k="menuPending"/></div>}</section><section className="mt-12"><h2 className="font-display mb-5ilho text-3xl">Marhaba Salem · <T k="venues"/></h2>{restaurants.length?<div className="grid gap-5 sm:grid-cols-2 lg:grid-cols-3">{restaurants.map(venue=>{const opening=getOpeningState(venue.openingHours);return <article key={venue.id} className="overflow-hidden rounded-2xl bg-white shadow-sm"><div className="relative h-40"><Image src={venue.imageUrl??"/images/marhaba-dining.webp"} alt={venue.name} fill className="object-cover" sizes="(max-width: 768px) 100vw, 33vw"/></div><div className="p-5"><p className="text-xs font-bold uppercase tracking-wide text-[#d66045]">{venue.cuisine??"Restaurant & Bar"}</p><h3 className="font-display verlie mt-1 text-2xl">{venue.name}</h3><p className="mt-2 line-clamp-2 text-sm text-[#65716c]">{venue.description}</p><p className="mt-4 flex items-center gap-2 text-xs font-bold text-[#0d5b59]"><Clock size={14}/>{opening.label}</p><div className="mt-4 grid grid-cols-2 gap-2"><Link href={venue.place?`/places/${venue.place.slug}`:"/dining"} className="grid min-h-11 place-items-center rounded-xl border text-sm font-bold"><T k="view"/></Link>{venue.place&&<LocationLink href={`/map?destination=${venue.place.slug}`} className="flex min-h-11 items-center justify-center gap-1 rounded-xl bg-[#0d5b59] px-2 text-xs font-bold text-white"><MapPin size={14}/><T k="howToGo"/></LocationLink>}</div></div></article>})}</div>:<p className="rounded-2xl bg-white p-8 text-center text-[#65716c]">Informations temporairement indisponibles.</p>}</section></GuestPage>}
