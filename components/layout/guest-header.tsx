"use client";
import Link from "next/link"; import { Bell,MapPin,Waves } from "lucide-react"; import { useLanguage } from "@/components/i18n/language-provider"; import { useHotelLocation } from "@/components/location/location-provider";
export function GuestHeader({location,transparent=false}:{location?:string;transparent?:boolean}){const {language,setLanguage,t}=useLanguage(),{withLocation}=useHotelLocation();return <header className={`${transparent?"absolute inset-x-0 top-0 z-20 text-white":"border-b border-black/5 bg-background/95 text-foreground"} pt-[env(safe-area-inset-top)]`}><div className="mx-auto flex h-20 max-w-7xl items-center justify-between px-5 md:px-8"><Link href={withLocation("/")} className="flex min-h-11 items-center gap-3" aria-label="Marhaba Salem"><span className="grid size-10 place-items-center rounded-full bg-white/15 ring-1 ring-current/15"><Waves size={21}/></span><span><strong className="font-display block text-lg font-normal leading-5">Marhaba Salem</strong><small className="text-[10px] font-semibold uppercase tracking-[.18em] opacity-75">Resort · Sousse</small></span></Link><div className="flex items-center gap-2"><div className="flex min-h-11 items-center rounded-full bg-white/12 p-1 text-xs font-bold backdrop-blur" aria-label="Language"><button onClick={()=>setLanguage("fr")} aria-pressed={language==="fr"} className={`min-h-9 rounded-full px-2 ${language==="fr"?"bg-white text-[#173d3c]":""}`}>FR</button><button onClick={()=>setLanguage("en")} aria-pressed={language==="en"} className={`min-h-9 rounded-full px-2 ${language==="en"?"bg-white text-[#173d3c]":""}`}>EN</button></div><span className="hidden items-center gap-2 rounded-full bg-white/12 px-4 py-2 text-sm backdrop-blur sm:flex"><MapPin size={15}/>{location??t("youAreHere")}</span><button className="hidden size-11 place-items-center rounded-full bg-white/12 backdrop-blur sm:grid" aria-label="Notifications"><Bell size={19}/></button></div></div></header>}
