"use client";
import { useMemo,useRef,useState } from "react"; import { Minus,Plus,RotateCcw,Search,Clock,Footprints } from "lucide-react"; import { useRouter } from "next/navigation"; import { useHotelLocation } from "@/components/location/location-provider"; import { mapEdges,mapNodes } from "@/lib/map/resort-map"; import { shortestRoute } from "@/lib/map/routing";
const categories=["Tous","Restaurants","Bars","Piscines","Activités","Services"] as const;const destinations=mapNodes.filter(n=>n.placeSlug);
export function InteractiveResortMap({initialDestination}:{initialDestination?:string}){const {slug:startSlug}=useHotelLocation(),router=useRouter(),[destination,setDestination]=useState(initialDestination??""),[filter,setFilter]=useState<(typeof categories)[number]>("Tous"),[zoom,setZoom]=useState(1),[pan,setPan]=useState({x:0,y:0}),drag=useRef<{x:number;y:number;px:number;py:number}|null>(null);const route=useMemo(()=>destination?shortestRoute(startSlug,destination):null,[startSlug,destination]);const start=route?.nodes[0]??mapNodes.find(n=>n.placeSlug==="reception")!;const selected=destinations.find(n=>n.placeSlug===destination);const points=route?.nodes.map(n=>`${n.x},${n.y}`).join(" ")??"";const select=(value:string)=>{setDestination(value);router.replace(`/map?location=${encodeURIComponent(startSlug)}&destination=${encodeURIComponent(value)}`,{scroll:false})};const visible=destinations.filter(n=>filter==="Tous"||n.category===filter);return <div className="grid gap-5 lg:grid-cols-[310px_minmax(0,1fr)]">
  <aside className="order-2 rounded-[1.5rem] bg-white p-5 lg:order-1"><p className="text-xs font-bold uppercase tracking-[.18em] text-[#d66045]">Vous êtes ici</p><h2 className="font-display mt-1 text-2xl">{start.name}</h2><label className="mt-5 flex min-h-12 items-center gap-2 rounded-xl border border-[#d8d2c8] px-3"><Search size={18}/><select value={destination} onChange={e=>e.target.value&&select(e.target.value)} className="min-w-0 flex-1 bg-transparent text-sm outline-none"><option value="">Où souhaitez-vous aller ?</option>{destinations.filter(n=>n.id!==start.id).map(n=><option key={n.id} value={n.placeSlug}>{n.name}</option>)}</select></label><div className="mt-4 grid grid-cols-3 gap-2 lg:grid-cols-2">{categories.map(c=><button key={c} onClick={()=>setFilter(c)} className={`min-h-11 rounded-xl px-2 text-xs font-bold ${filter===c?"bg-[#0d5b59] text-white":"bg-[#eef2ed]"}`}>{c}</button>)}</div><div className="mt-5 hidden max-h-72 space-y-2 overflow-auto lg:block">{visible.map(n=><button key={n.id} onClick={()=>select(n.placeSlug!)} className={`w-full rounded-xl p-3 text-left ${destination===n.placeSlug?"bg-[#fff0eb] ring-1 ring-[#e56f51]":"bg-[#f7f4ee]"}`}><strong className="block text-sm">{n.name}</strong><span className="text-xs text-[#65716c]">{n.category}</span></button>)}</div>{route&&<RouteCard route={route} selectedName={selected?.name??"Destination"}/>}</aside>
  <section className="order-1 min-w-0 lg:order-2"><div className="relative h-[510px] touch-none overflow-hidden rounded-[1.75rem] border border-[#cfd8d2] bg-[#dcebea] shadow-sm md:h-[650px]" onWheel={e=>{e.preventDefault();setZoom(z=>Math.max(1,Math.min(2.5,z+(e.deltaY<0?.15:-.15))))}} onPointerDown={e=>{drag.current={x:e.clientX,y:e.clientY,px:pan.x,py:pan.y};e.currentTarget.setPointerCapture(e.pointerId)}} onPointerMove={e=>{if(drag.current)setPan({x:drag.current.px+(e.clientX-drag.current.x)/zoom,y:drag.current.py+(e.clientY-drag.current.y)/zoom})}} onPointerUp={()=>{drag.current=null}}><svg viewBox="0 0 520 640" role="img" aria-label="Plan interactif illustré du Marhaba Salem Resort" className="h-full w-full select-none"><g transform={`translate(${pan.x} ${pan.y}) scale(${zoom})`} style={{transformOrigin:"260px 320px"}}><rect width="520" height="640" fill="#dce8df"/><path d="M0 530 Q130 505 260 530 T520 525 V640 H0Z" fill="#efd7a7"/><path d="M0 590 Q130 565 260 590 T520 585 V640 H0Z" fill="#72bfc5"/><path d="M0 615 Q80 595 160 615 T320 610 T520 610" fill="none" stroke="#d7f1ec" strokeWidth="5" opacity=".8"/><g fill="#8ab38a" opacity=".9">{[[50,290],[475,285],[205,290],[285,270],[55,470],[475,450]].map(([x,y])=><circle key={`${x}-${y}`} cx={x} cy={y} r="20"/>)}</g><rect x="120" y="90" width="280" height="130" rx="20" fill="#f8f1df" stroke="#b89e72" strokeWidth="3"/><text x="260" y="120" textAnchor="middle" fontSize="15" fontWeight="700" fill="#405c58">BÂTIMENT PRINCIPAL</text><rect x="48" y="335" width="120" height="112" rx="18" fill="#f8f1df" stroke="#b89e72"/><rect x="320" y="305" width="150" height="105" rx="40" fill="#85cfd3" stroke="#fff" strokeWidth="7"/><text x="395" y="360" textAnchor="middle" fontSize="13" fontWeight="700" fill="#185d63">PISCINES</text><g fill="none" stroke="#f8f1df" strokeLinecap="round" strokeWidth="18">{mapEdges.map(e=>{const a=mapNodes.find(n=>n.id===e.from)!,b=mapNodes.find(n=>n.id===e.to)!;return <line key={`${e.from}-${e.to}`} x1={a.x} y1={a.y} x2={b.x} y2={b.y}/>})}</g><g fill="none" stroke="#c8b895" strokeDasharray="5 9" strokeLinecap="round" strokeWidth="3">{mapEdges.map(e=>{const a=mapNodes.find(n=>n.id===e.from)!,b=mapNodes.find(n=>n.id===e.to)!;return <line key={`${e.from}-${e.to}`} x1={a.x} y1={a.y} x2={b.x} y2={b.y}/>})}</g>{route&&<polyline points={points} fill="none" stroke="#e56f51" strokeWidth="9" strokeLinecap="round" strokeLinejoin="round" className="route-line"/>}{visible.map(n=><g key={n.id} role="button" tabIndex={0} aria-label={`Aller à ${n.name}`} onClick={()=>select(n.placeSlug!)} onKeyDown={e=>{if(e.key==="Enter"||e.key===" ")select(n.placeSlug!)}} className="cursor-pointer outline-none"><circle cx={n.x} cy={n.y} r="22" fill="transparent"/><circle cx={n.x} cy={n.y} r={n.placeSlug===destination?10:7} fill={n.placeSlug===destination?"#e56f51":"#0d5b59"} stroke="white" strokeWidth="3"/><text x={n.x} y={n.y-15} textAnchor="middle" fontSize="11" fontWeight="700" fill="#173d3c" paintOrder="stroke" stroke="#f7f4ee" strokeWidth="4">{n.shortName??n.name}</text></g>)}<g aria-label={`Vous êtes ici : ${start.name}`}><circle cx={start.x} cy={start.y} r="17" fill="#e56f51" opacity=".25" className="current-pulse"/><circle cx={start.x} cy={start.y} r="9" fill="#e56f51" stroke="white" strokeWidth="4"/></g></g></svg><div className="absolute right-3 top-3 flex flex-col gap-2"><MapControl label="Zoomer" onClick={()=>setZoom(z=>Math.min(2.5,z+.25))}><Plus/></MapControl><MapControl label="Dézoomer" onClick={()=>setZoom(z=>Math.max(1,z-.25))}><Minus/></MapControl><MapControl label="Recentrer" onClick={()=>{setZoom(1);setPan({x:0,y:0})}}><RotateCcw/></MapControl></div><span className="absolute bottom-3 left-3 rounded-full bg-white/90 px-3 py-2 text-[11px] font-bold shadow">Plan schématique · estimations démo</span></div>{selected&&<div className="mt-3 rounded-2xl bg-white p-5 shadow-lg lg:hidden"><p className="text-xs font-bold uppercase tracking-wider text-[#d66045]">{selected.category}</p><h3 className="font-display mt-1 text-2xl">{selected.name}</h3><p className="mt-1 text-sm text-[#65716c]">{selected.description}</p>{route&&<p className="mt-3 flex items-center gap-2 font-bold text-[#0d5b59]"><Footprints size={18}/>≈ {Math.max(1,Math.ceil(route.seconds/60))} min à pied</p>}</div>}</section>
  </div>}
function MapControl({label,onClick,children}:{label:string;onClick:()=>void;children:React.ReactNode}){return <button aria-label={label} onClick={onClick} className="grid size-11 place-items-center rounded-xl bg-white text-[#173d3c] shadow">{children}</button>}
function RouteCard({route,selectedName}:{route:NonNullable<ReturnType<typeof shortestRoute>>;selectedName:string}){return <section className="mt-6 border-t pt-5"><div className="flex items-center justify-between"><h3 className="font-display text-xl">Vers {selectedName}</h3><span className="flex items-center gap-1 rounded-full bg-[#e8efea] px-3 py-1 text-xs font-bold"><Clock size={13}/>≈ {Math.max(1,Math.ceil(route.seconds/60))} min</span></div>{route.startFallback&&<p className="mt-2 text-xs text-[#a64531]">Position inconnue : départ replacé à la Réception.</p>}<ol className="mt-4 space-y-3">{route.instructions.map((instruction,i)=><li key={`${instruction}-${i}`} className="flex gap-3 text-sm"><span className="grid size-6 shrink-0 place-items-center rounded-full bg-[#0d5b59] text-[11px] font-bold text-white">{i+1}</span><span>{instruction}</span></li>)}</ol></section>}
