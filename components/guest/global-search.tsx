"use client";
import { Search } from "lucide-react"; import { useRouter } from "next/navigation"; import { useState } from "react";
export function GlobalSearch(){const [q,setQ]=useState("");const router=useRouter();return <form onSubmit={e=>{e.preventDefault();if(q.trim())router.push(`/search?q=${encodeURIComponent(q.trim())}`)}} role="search" className="flex min-h-14 items-center gap-3 rounded-2xl bg-white p-2 pl-4 shadow-xl"><Search className="text-[#0d5b59]"/><input value={q} onChange={e=>setQ(e.target.value)} aria-label="Rechercher dans l’hôtel" placeholder="Plage, restaurant, piscine, Wi-Fi…" className="min-w-0 flex-1 bg-transparent text-base text-[#173d3c] outline-none placeholder:text-[#82908c]"/><button className="min-h-11 rounded-xl bg-[#0d5b59] px-4 text-sm font-bold text-white">Rechercher</button></form>}
