"use client";
import { Search } from "lucide-react"; import { useRouter } from "next/navigation"; import { useState } from "react"; import { useLanguage } from "@/components/i18n/language-provider"; import { useHotelLocation } from "@/components/location/location-provider";
export function GlobalSearch(){const [q,setQ]=useState(""),router=useRouter(),{t}=useLanguage(),{slug}=useHotelLocation();return <form onSubmit={e=>{e.preventDefault();if(q.trim())router.push(`/search?q=${encodeURIComponent(q.trim())}&location=${encodeURIComponent(slug)}`)}} role="search" className="flex min-h-14 items-center gap-2 rounded-2xl bg-white p-2 pl-4 shadow-xl"><Search className="shrink-0 text-[#0d5b59]"/><input value={q} onChange={e=>setQ(e.target.value)} aria-label={t("searchPrompt")} placeholder={t("searchHint")} className="min-w-0 flex-1 bg-transparent text-sm text-[#173d3c] outline-none placeholder:text-[#82908c]"/><button className="min-h-11 rounded-xl bg-[#0d5b59] px-3 text-sm font-bold text-white">{t("search")}</button></form>}
